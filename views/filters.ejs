<%- include('includes/head.ejs') %>
<link rel="stylesheet" href="/css/forms.css">
<link rel="stylesheet" href="/css/product.css">
</head>

<body style="background-color:white">

<main>
    <form action="/filters" method="post">
    <label for="genre">
        <font style="color:red;font-size:15px" >Genre</font>
    </label><br>
	<% for(var i=0; i < genre.length; i++) { %>
        <% if(fgenre.includes(genre[i])) { %>
            <input type="checkbox" name="genre" value="<%=genre[i]%>" checked>
            <label for="genre"><%=genre[i]%></label><br>
        <% } else { %>
            <input type="checkbox" name="genre" value="<%=genre[i]%>">
            <label for="genre"><%=genre[i]%></label><br>
        <% } %>
    <% } %>    
    <label for="low"><font style="color:red;font-size:15px" >IMDb Rating lowest</font></label>
        <input type="text" name="low" id="low" value="">
    <br><label for="high"><font style="color:red;font-size:15px" >IMDb Rating highest</font></label>
        <input type="text" name="high" id="high" value="">
    <br><button class="btn" type="submit">Apply</button>
    </form>
</main>
<%- include('includes/end.ejs') %>